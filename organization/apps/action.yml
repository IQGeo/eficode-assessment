name: 'Organization GitHub Apps'
description: 'Get Organization GitHub Apps'

inputs:
  org:
    description: 'Organization name'
    required: true

runs:
  using: "composite"
  steps:
    - run: ./script.sh ${{ inputs.org }}
      shell: bash
      working-directory: ./organization/apps
