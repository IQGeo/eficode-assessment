name: 'Github Secrets'
description: 'Get organisation, dependabot, codespaces and actions secrets'

inputs:
  org:
    description: 'Organization name'
    required: true

runs:
  using: "composite"
  steps:
    - run: ./script.sh ${{ inputs.org }}
      shell: bash
      working-directory: ./repository/secrets
